<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wordclock"> <!-- Replace with your actual package name -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bg"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config" ><!-- Include network security config here -->

        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Add this line -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
